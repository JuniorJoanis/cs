# NGINX and Postfix setup.

  You need to clone this repository into the bridge server.
  
  Then you will have to run this command:
  
    `./setup.rb`

 NB: Make sure to have ruby installed into the bridge server 